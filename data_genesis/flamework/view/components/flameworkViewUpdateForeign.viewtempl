<ion-item>
    <ion-select placeholder="Sélectionner un choix" cFormControl id="[fieldNameMin]" name="[fieldNameMin]" [(ngModel)]="updated[classNameMaj].[fieldNameMin]">
        <ion-select-option *ngFor="let [foreignNameMin] of [foreignNameMin]s"  [ngValue]="[foreignNameMin]">{{ [foreignNameMin].[foreignFieldGet] }}
        </ion-select-option>
    </ion-select> 
    <ion-label cLabel for="[fieldNameMin]">
        [foreignNameFormattedMaj]
    </ion-label> 
</ion-item>